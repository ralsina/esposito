#ifndef PLATFORM_COMPAT_H
#define PLATFORM_COMPAT_H

// Platform compatibility for Linux emulation of ESP32-specific code.
// ESP32 inline asm (rsr ccount, etc.) is stripped from app sources via sed
// in the Makefile before compilation.

#endif
